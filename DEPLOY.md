# 部署指南

本站已部署到 Cloudflare Pages，详见 [`CLAUDE.md` 第 8 节](./CLAUDE.md#8-部署配置已完成仅作记录)。

## 日常更新

```bash
git add .
git commit -m "your message"
git push
```

推送到 `main` 分支后 Cloudflare Pages 会自动 build & deploy，约 1–2 分钟生效。

## 中国大陆搜索引擎提交

参考 [`CN-submission.md`](./CN-submission.md)。
